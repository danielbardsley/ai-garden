# Decisions

- Removed unused bootstrap placeholder modules for assistant, gallery, plant records, and permissions because real implementations now exist under `garden-agent`, `garden-design`, and `garden-records`.
- Replaced stale bootstrap README/product TBD language with current implemented scope plus explicit deferred capabilities.
- Kept generated placeholder photo records in web/demo seed data because they are still the active visual fallback for seeded records without real captured image files.
- Kept TextInput placeholder props and not-yet-built reminders/profile/media-import copy because those placeholders still reflect active UI affordances or deferred features.
