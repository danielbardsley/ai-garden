# Gallery Card Subheader Decisions

- The plant name should appear once per Gallery card/row.
- The subheader should answer “what is useful about this plant/photo?” rather than repeat identity.
- Use absolute short dates in Gallery instead of relative offsets to avoid confusing negative values from future/fixture dates.
- Use local photo count and latest captured date because they are available without schema changes.
