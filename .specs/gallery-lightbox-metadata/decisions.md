# Gallery Lightbox Metadata Decisions

- Raw IDs are developer/internal data and should not be shown as the secondary lightbox header.
- Use photo source plus saved-to-plant context as the secondary line for now.
- Keep relative day labels out of Gallery headers to avoid confusing negative offsets from fixture dates.
